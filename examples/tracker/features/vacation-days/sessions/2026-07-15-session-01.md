# Session 01 — 2026-07-15

## Summary
Full feature cycle in one session: spec (enhance-prompt interview) → lane scaffold → design
workflow (Map→Design→Attack, 435k tokens) → owner approved defaults → implementation workflow
(implement → independent review + verify; one resume after a session-limit outage killed the
review seats) → commit 3d360a7 → draft PR #8.

## Accomplished
- Enhanced prompt with 6 requirements (business rules 1–3 + confirmed decisions).
- Lane scaffolded and set active; the lane design doc written (design + 10 critique findings).
- Feature implemented in worktree vacation-days (scheduler.py 752 lines touched, app.py +58,
  CLAUDE.md landmines updated); reviewer approved 0 issues; verifier passed all 14 battery
  checks incl. grid-for-grid no-leave bit-identity vs main @ 25e29ff. Draft PR #8 opened.

## Findings & learnings
- max-consec-OFF window constraint (scheduler.py:589-591) makes any leave ≥ max_consec_off+1
  days INFEASIBLE unless run rules go per-available-segment — verified by reading the constraint.
- app.py:240 paints FAIL badges with WEB_COLORS[OFF]; blanking OFF blanks FAIL badges — verified.
- Displayed OFF label is "OFF" via CELL_LABEL (scheduler.py:1042), not the internal "O" — verified.
- Critics killed 3 major design flaws pre-implementation (see the lane design doc amendments): pinned-crew
  run-cap preflight gap; alternation chain infeasible for target-0 leave-takers; grid extraction
  masking hard-block violations (validator check would score labeling, not the constraint).

## Decisions (+ why)
- **Hours-prorated targets: required = 16 − leave_days** — owner picked; matches business "192h" framing.
- **Excel totals become live COUNTIF formulas** — owner picked; hand-edits must recount (rule 2).
- **Leave = grid cells (red FFC7CE) + Staff-on-leave table** — owner picked (rule 3 "similar to night shift").
- **Leave entry: multiple (start,end) ranges per employee** — owner picked.
- **LEAVE = "V" third grid code; per-segment run rules; weighted F2/F3 when leave present** — design
  seat's committed architecture, survived adversarial review (with amendments).

## Blockers / open questions
- **Owner decisions pending** (the lane design doc §8): drop "OFF" text? AM staff leave in v1? rotate-mode
  support? label "V" vs "L"? F4 stays raw? — *unblocks when:* owner answers or accepts defaults.

## Verification state
- **Written:** PR body/commit message.
- **Verified:** independent verifier re-ran the full 14-item battery (all PASS, real outputs);
  independent reviewer verdict approve/0 issues; bit-identity element-wise equal on all six
  smoke configs; commit-message sweep clean.

## Post-merge addendum (owner said "merge and clean up")
- main had moved (PR #7 wizard redesign, 5f4e9fe) → app.py conflict; a resolve→review→verify
  workflow re-implemented the leave UI inside the wizard Setup step (merge 8b2f69a), re-verified
  14/14 vs the NEW main. Review: 2 minors — new-month leave carryover FIXED pre-merge (d0453ba,
  AppTest-verified), mid-Setup reseed loss DEFERRED → ISS-001.
- PR #8 squash-merged as b669b2a; remote+local branches and worktree deleted; main checkout pulled.

## Next steps (ordered; #1 runnable immediately by a cold reader)
1. Nothing required. Optional: vacation-days/ISS-001, or v2 AM-staff leave.
